<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.baomidou</groupId>
    <artifactId>mybatis-plus-solon-parent</artifactId>
    <version>3.5.17</version>
    <packaging>pom</packaging>

    <modules>
        <module>mybatis-plus-solon-plugin</module>
        <module>mybatis-plus-solon3-plugin</module>
        <module>mybatis-plus-solon-native-image</module>
    </modules>

    <properties>
        <!-- solon 3.x LTS 线 -->
        <solon3.version>3.7.5</solon3.version>
        <!-- solon 4.x 主线 -->
        <solon4.version>4.0.3</solon4.version>
    </properties>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <source>8</source>
                        <target>8</target>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

</project>
